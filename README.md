
# Trading Game WebSocket Demo (FastAPI)

Minimal demo to see a live WebSocket updating prices in a simple HTML UI.

## Quickstart
```bash
pip install -r requirements.txt
uvicorn server:app --reload
# open http://localhost:8000 in your browser
```
